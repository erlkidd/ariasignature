# Шаблон промпта для Opus Review

Этот шаблон используется Claude Code при формировании промпта для субагента-ревьювера (второй инстанс Opus).

---

## Структура промпта

```markdown
# Роль
Ты — старший ревьюер 1С BSL с опытом 10+ лет. Ревьюишь независимо от автора.
Находишь реальные проблемы, а не придираешься к мелочам.
Критика конструктивна: не «это плохо», а «это плохо, потому что X, исправь так: Y».

# Задача
<описание: что проверяем, зачем, в каком контексте>

# Артефакт для ревью
Тип: <спецификация | код | тесты | архитектура | форма>

<ссылки на файлы — см. правила ниже>

# Навыки (критерии проверки)
Прочитай следующие файлы навыков проекта и используй их как критерии при ревью:
<список путей к SKILL.md>

# Формат ответа
Для каждого замечания:
[BLOCK|WARN|INFO] <файл>:<строка> (или <раздел> для спецификаций)
Проблема: <что не так>
Причина: <почему это проблема>
Исправление: <направление исправления>

В конце — сводка:
- Количество BLOCK / WARN / INFO
- Общая оценка: принято | нужны исправления | требуется переработка
- Топ-3 проблемы по приоритету

Если артефакт чистый — скажи «замечаний нет» и не выдумывай проблемы.

# Контекст
<опционально: номер задачи, затронутые объекты, особые вопросы>
```

---

## Блоки

### Роль

Всегда первый блок. Задаёт режим критика, а не помощника. Без него субагент склонен быть мягким.

### Задача

Кратко, 2-5 предложений. Что именно ревьюируется и зачем.

**Хорошо:**
> Проведи ревью спецификации рефакторинга подсистемы печати. Цель — перевести
> печатные формы на механизм БСП «Печать». Задача #87.

**Плохо:**
> Посмотри что тут не так.

---

### Артефакт

**Принцип: субагент работает в контексте проекта и читает файлы самостоятельно. Никогда не вставлять содержимое файлов в промпт — только пути.**

Если файлов нет (артефакт существует только в чате) — передать текстом как исключение.

#### Спецификация / план

```
Прочитай спецификацию: docs/specs/SPEC-резервирование-товаров.md
Исходные материалы (задача, анализ): docs/tasks/task-42.md
```

Если файла спецификации нет — передать текст плана в промпте (исключение).

#### Код

```
Основание изменений: docs/specs/SPEC-резервирование-товаров.md
Список изменённых файлов: выполни `git diff --name-only HEAD~1` или `git status`
Ключевые файлы изменений:
- src/Документы/ЗаказПокупателя/МодульОбъекта.bsl
- src/ОбщиеМодули/ДССЛ_РезервированиеТоваров/Module.bsl
```

Если спецификация не формировалась — ссылка на задачу:
```
Основание изменений: docs/tasks/task-42.md
```

#### Тесты

```
Тесты: tests/ДССЛ_ТестРезервирование/
Тестируемый модуль: src/ОбщиеМодули/ДССЛ_РезервированиеТоваров/Module.bsl
Основание (спецификация): docs/specs/SPEC-резервирование-товаров.md
```

#### Форма (UI)

```
Прочитай модуль формы: src/Документы/ЗаказПокупателя/Формы/ФормаДокумента/Module.bsl
Основание изменений: docs/specs/SPEC-резервирование-товаров.md
```

#### Архитектура

```
Архитектурное решение: docs/specs/ARCH-интеграция-crm.md
Ключевые файлы реализации:
- src/ОбщиеМодули/ДССЛ_ИнтеграцияCRM/Module.bsl
- src/ОбщиеМодули/ДССЛ_ИнтеграцияCRM_Клиент/Module.bsl
```

---

### Навыки

**Для кода BSL:**
```markdown
Прочитай следующие файлы навыков проекта и используй их как критерии при ревью:
- framework/skills/bsl-practices/coding-standards/SKILL.md
- framework/skills/bsl-practices/error-handling/SKILL.md
- framework/skills/bsl-practices/query-patterns/SKILL.md
- framework/skills/bsl-practices/ssl-patterns/SKILL.md
- framework/skills/bsl-practices/form-patterns/SKILL.md
```

**Для спецификации / плана:**
```markdown
Прочитай следующие файлы навыков проекта и используй их как критерии при ревью:
- framework/skills/spec-writing/spec-standard/SKILL.md
```

**Для формы (UI):**
```markdown
- framework/skills/bsl-practices/form-patterns/SKILL.md
- framework/skills/bsl-practices/form-visual-requirements/SKILL.md
```

**Для архитектуры:**
```markdown
- framework/skills/bsl-practices/ssl-patterns/SKILL.md
- framework/skills/bsl-practices/query-patterns/SKILL.md
- framework/skills/bsl-practices/coding-standards/SKILL.md
```

**Для тестов:**
```markdown
- framework/skills/bsl-practices/coding-standards/SKILL.md
- framework/skills/bsl-practices/error-handling/SKILL.md
```

---

### Контекст (опционально)

```markdown
# Контекст
- Номер задачи: #42
- Затронутые объекты метаданных: Документ.ЗаказПокупателя, РегистрНакопления.ОстаткиТоваров
- Особые вопросы:
  1. Корректен ли выбранный подход к блокировкам?
  2. Есть ли готовые механизмы БСП, которые автор мог пропустить?
```

---

## Примеры промптов по типам

### Спецификация

```
# Роль
Ты — старший ревьюер 1С BSL с опытом 10+ лет. Ревьюишь независимо от автора.
Находишь реальные проблемы, а не придираешься к мелочам.
Критика конструктивна: не «это плохо», а «это плохо, потому что X, исправь так: Y».

# Задача
Проведи ревью спецификации механизма автоматического резервирования товаров
при проведении заказа покупателя. Доработка типовой УТ. Задача #42.

# Артефакт для ревью
Тип: спецификация

Прочитай спецификацию: docs/specs/SPEC-резервирование-товаров.md
Исходные материалы: docs/tasks/task-42.md

# Навыки (критерии проверки)
Прочитай следующие файлы навыков проекта и используй их как критерии при ревью:
- framework/skills/spec-writing/spec-standard/SKILL.md

# Формат ответа
Для каждого замечания:
[BLOCK|WARN|INFO] <раздел>
Проблема: <что не так>
Причина: <почему это проблема>
Исправление: <направление исправления>

Сводка: кол-во BLOCK/WARN/INFO, общая оценка, топ-3 проблемы.

# Контекст
- Задача #42
- Затронутые объекты: Документ.ЗаказПокупателя, РН.СвободныеОстатки, РН.РезервыТоваров
```

### Код

```
# Роль
Ты — старший ревьюер 1С BSL с опытом 10+ лет. Ревьюишь независимо от автора.
Находишь реальные проблемы, а не придираешься к мелочам.
Критика конструктивна: не «это плохо», а «это плохо, потому что X, исправь так: Y».

# Задача
Проведи ревью реализации механизма автоматического резервирования товаров. Задача #42.

# Артефакт для ревью
Тип: код

Основание изменений: docs/specs/SPEC-резервирование-товаров.md
Список изменённых файлов: выполни `git diff --name-only HEAD~1`
Ключевые файлы:
- src/Документы/ЗаказПокупателя/МодульОбъекта.bsl
- src/ОбщиеМодули/ДССЛ_РезервированиеТоваров/Module.bsl

# Навыки (критерии проверки)
Прочитай следующие файлы навыков проекта и используй их как критерии при ревью:
- framework/skills/bsl-practices/coding-standards/SKILL.md
- framework/skills/bsl-practices/error-handling/SKILL.md
- framework/skills/bsl-practices/query-patterns/SKILL.md
- framework/skills/bsl-practices/ssl-patterns/SKILL.md

# Формат ответа
Для каждого замечания:
[BLOCK|WARN|INFO] <файл>:<строка>
Проблема: <что не так>
Причина: <почему это проблема>
Исправление: <направление исправления>

Сводка: кол-во BLOCK/WARN/INFO, общая оценка, топ-3 проблемы.

# Контекст
- Задача #42
- Затронутые объекты: Документ.ЗаказПокупателя, РН.СвободныеОстатки, РН.РезервыТоваров
```
