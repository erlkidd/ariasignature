# Changelog

Все значимые изменения в проекте фиксируются в этом файле.

Формат основан на категориях: `Added`, `Changed`, `Fixed`, `Docs`.

## [Unreleased]

### Fixed

- UI (`MainWindow`): меньше ложных жёстких ошибок при старте — временные сбои WebView2 к локальному API обрабатываются через восстановление; `EnsureDefaultAutostartApplied` после инициализации WebView, даже если SPA не прислала `appReady`; часть обновлений UI на `DispatcherPriority.Background`.

### Docs

- В документации и пользовательских формулировках API везде используется термин **токен** (удалённого API) вместо «секрет».

## [1.0.0] - 2026-05-02

### Added

- Служба: привязка HTTP API ко **всем интерфейсам** по умолчанию (`Api:Bind=all`: `0.0.0.0` + IPv6); режим только localhost (`loopback`).
- Настройки SQLite/UI: `apiBind`, `apiSharedSecret`; опциональная проверка `Authorization: Bearer` / `X-Aria-Api-Key` для запросов не с loopback.
- API: middleware авторизации; теги Swagger (`Service`, `System`, `Disks`, `Backups`, `Settings`); описание `GET /system` как соответствие вкладке «О системе».
- Установщик: правило брандмауэра Windows для входящего **TCP 5160** (и удаление при деинсталляции).
- Интеграционные тесты для `GET /system` и `GET /settings`; скрипт `scripts/api-http-smoke.ps1` для локальной HTTP-проверки.

### Changed

- UI: подписи в настройках — «токен для удалённого API»; текст про заголовки `Authorization` / `X-Aria-Api-Key` без отдельного пояснения про локальную панель.
- Версия продукта **1.0.0** (сборки .NET, установщик Inno Setup, SPA).

### Docs

- `README.md`, `API.md`, `USER_GUIDE.md`, `INSTRUCTIONS.md`, `INSTALLER.md`, `DEVELOPER_GUIDE.md`, `RELEASE_GATE`: сценарий централизованного опроса по IP/VPN, брандмауэр; шапки документов **1.0.0**.

## [0.9.9] - 2026-05-02

### Changed

- UI: при старте панели дольше показывается оверлей/спиннер без «ошибки API»; жёсткая страница диагностики только при долгом отсутствии ответа при запущенной службе, остановленной службе после ожидания или недоступности SCM.
- UI: опрос API при старте с коротким HTTP-таймаутом; фоновый запуск службы до 30 с без преждевременного MessageBox.
- Служба: параллельный старт hosted-сервисов (`ServicesStartConcurrently`); чтение порта API из БД — таймаут 2 с.
- Автозапуск панели: галочка в настройках управляет **HKCU Run** и **ярлыком** в `shell:startup` (`--tray`); при первом запуске после установки автозапуск включается один раз, если ещё не настроен; установщик не создаёт ярлык в автозагрузке и не регистрирует задачу планировщика для UI (удаление старой задачи при деинсталляции сохранено).
- Версия продукта **0.9.9**.

## [0.9.8] - 2026-05-02

### Fixed

- Автозапуск панели после входа в Windows: установщик снова создаёт ярлык **`AriaSignature`** в папке автозагрузки **текущего пользователя** (`shell:startup`) с аргументом `--tray` (основной механизм). Задача планировщика **AriaSignatureTrayLogon** остаётся запасным вариантом с `/IT` и явным `/RU` «домен\пользователь» при успешном создании.
- Версия продукта **0.9.8**.

### Docs

- `USER_GUIDE`: описание двух механизмов автозапуска UI (Startup + планировщик).

## [0.9.7] - 2026-05-02

### Changed

- Автозапуск панели в трее: при установке регистрируется задача планировщика **AriaSignatureTrayLogon** (вход в систему, задержка 45 с, `--tray`); общий ярлык в папке «Автозагрузка» убран; при обновлении удаляется старый `AriaSignature.lnk` из общей автозагрузки.
- UI: иконка в области уведомлений создаётся **до** инициализации главного окна/WebView2; при старте с `--tray` пишется строка в `%LocalAppData%\AriaSignature\logs\ui-boot.log`.
- Версия продукта **0.9.7** (сборки .NET, установщик Inno Setup, SPA, константа UI).

### Docs

- `USER_GUIDE`: планировщик, область «скрытых» значков Windows 11, лог `ui-boot.log`.

## [0.9.6] - 2026-05-02

### Changed

- Автозапуск панели в настройках: только запись в профиль пользователя (HKCU Run), **без** UAC и без `sc config`; тип запуска службы по-прежнему задаётся **при установке** (`start= auto`).
- Установщик: ярлык «AriaSignature» в общей папке автозагрузки (`--tray`) создаётся **всегда** (в т.ч. при тихой установке), отдельная задача мастера для этого убрана.
- Версия продукта **0.9.6** (сборки .NET, установщик Inno Setup, SPA, константа UI).

### Docs

- `USER_GUIDE`: гарантированный сценарий после установки, без ручного реестра.

## [0.9.5] - 2026-05-02

### Fixed

- Автозапуск в настройках: запись в автозагрузку пользователя (HKCU Run) выполняется **до** запроса UAC для `sc config`; отмена UAC больше не блокирует запуск панели при входе в Windows.

### Changed

- Сообщение `autostart` в WebView2 дополняется полем `serviceBootAuto`; в настройках показывается предупреждение, если панель в автозапуске, а служба не в режиме Automatic.
- Версия продукта **0.9.5** (сборки .NET, установщик Inno Setup, SPA, константа UI).

### Docs

- `USER_GUIDE`: порядок применения автозапуска панели и службы, отмена UAC.

## [0.9.4] - 2026-05-02

### Changed

- Настройка **«Запускать при входе в Windows»** в панели: синхронизированы автозапуск UI в трее (HKCU Run) и тип запуска службы `AriaSignatureService` («Автоматически» / «Вручную» через `sc config`, с подтверждением UAC).
- Версия продукта **0.9.4** (сборки .NET, установщик Inno Setup, SPA, константа UI).

### Docs

- `USER_GUIDE`: флажок автозапуска и служба Windows.

## [0.9.3] - 2026-05-02

### Added

- Файловый журнал службы: `%ProgramData%\AriaSignature\logs\service-*.log` (Serilog `WriteTo.File`, суточная ротация, до 14 файлов).

### Changed

- Версия продукта **0.9.3** (сборки .NET, установщик Inno Setup, SPA, константа UI).
- Установщик: задача **«Запускать AriaSignature при входе в Windows (в трее)»** по умолчанию **включена** (скрипт `CurPageChanged`: флаг `checked` в `[Tasks]` не поддерживается компилятором Inno в этой среде); у ярлыков UI задан **рабочий каталог** `{app}\ui`.

### Docs

- `USER_GUIDE`: разъяснены роли службы и UI, трей, автозапуск и путь к логам службы.

### Notes

- Иконка в трее принадлежит только процессу панели (`AriaSignature.UI`); фоновый сервис её не отображает.

## [0.9.2] - 2026-05-02

### Fixed

- Панель открывается с **localhost** в WebView2; статический `logo.png` мог долго отдаваться из кэша — для файлов из `wwwroot` выставлены заголовки **без долговременного кэша**, у URL логотипа/favicon после каждой сборки Vite добавляется уникальный query-параметр.
- Публикация службы: содержимое **`wwwroot`** при `dotnet publish` копируется с **Always**, чтобы в установке не залипали старые PNG/JS/CSS после обновления.

### Changed

- Версия продукта **0.9.2**.

## [0.9.1] - 2026-05-02

### Fixed

- Единый брендинг **`assets/branding/logo.png`**: при сборке SPA генерируется `icon.ico` для exe/трея/установщика; логотип в интерфейсе и окне приложения берётся из того же файла.

### Changed

- Версия продукта **0.9.1** (сборки .NET, установщик Inno Setup, SPA, константа UI).
- Ссылка «MIT» на странице «О системе» ведёт на `https://github.com/erlkidd/ariasignature/blob/production/LICENSE`.

### Docs

- Ветка **production** приведена к содержимому **test/agent-work** (первый рабочий релиз в основной ветке).

## [0.9.0] - 2026-05-02

### Changed

- Версия продукта **0.9.0** (сборки .NET, установщик Inno Setup, SPA, константа UI).

### Docs

- Корневой `README.md` с логотипом и описанием для GitHub; актуализация шапок документов под релиз.

### Notes

- Установщик `AriaSignature-Setup.exe` публикуется на **GitHub Releases** для скачивания без сборки из исходников.

## [0.8.5] - 2026-05-02

### Changed

- Типографика и вертикальный ритм интерфейса: единая шкала кегля, отступы между секциями и блоками панели.
- Вкладка «О системе»: сведения в таблицах параметр / значение, секции в визуальных блоках.
- Активные задачи: колонка и подпись поля «Папка архива» (вместо «Папка архивов» там, где показывается путь архива).
- Настройки: подпись «Обновление дисков» вместо «Обновление SMART»; краткая подсказка для исходящей синхронизации; расписание POST с теми же пресетами, что и опрос дисков (расширенный Quartz отдельно).
- Исходящий POST на коллектор без `Authorization` и без пользовательских заголовков из настроек; соответствующие поля убраны из API и UI.

### Fixed

- Файловая архивация `.1CD`: внутри RAR файл называется как у источника (например `1Cv8.1CD`), без префикса GUID во временной копии.

### Docs

- Обновлены `API.md`, `USER_GUIDE.md`, `DEVELOPER_GUIDE.md`, версии в сопутствующих документах.

## [0.7.0] - 2026-05-02

### Added
- Вкладка «О системе» (перед «Диски»): hostname, DNS, адреса сетевых интерфейсов, ОС, CPU, ОЗУ, видеокарты (Windows/WMI).
- `GET /api/v1/system` — JSON-снимок системы для UI и интеграций.
- Исходящая синхронизация на коллектор: настройки в SQLite/UI (`OutboundSync:*`), Quartz job периодически выполняет `POST` на заданный URL с телом JSON (`system`, `disks`, `backups.jobs`, `backups.recentLogs`). Опционально: Bearer token и один пользовательский заголовок (имя + значение). Локальный входящий API по-прежнему без этих токенов.
- Перепланирование cron исходящей отправки при `PUT /settings` и синхронизация триггера с БД при старте службы.

### Docs
- Обновлены `API.md`, `USER_GUIDE.md`, `DEVELOPER_GUIDE.md`, `DEVELOPMENT_NOTES.md` и версии сопутствующих документов.

## [0.3.0] - 2026-05-02

### Changed
- Расписание опроса дисков по умолчанию: раз в час (`0 0 * * * ?`); прогрев при старте службы обновляет только таблицу дисков без записи в историю `SmartMetrics`; кнопка «Обновить данные», Quartz и расписание по-прежнему добавляют точки истории.
- Усечение истории SMART: до ~200 последних записей на диск и общий потолок 1200 строк.
- Панель: вкладка «Активные задачи» включает текущий запуск даже при отключённой задаче; нейтральный пустой экран; статусы архивации в интерфейсе на русском; построение и разбор cron для дисков (в т.ч. «каждый час» без неверного `*/60`); подсказки для ручного Quartz-cron.
- MSSQL: для `BACKUP DATABASE` снято ограничение времени выполнения команды; сообщение о SQL-таймауте формулируется по-русски.
- Окно панели: при старте сразу показывается экран «Загрузка…» вместо пустого белого WebView2 до готовности API.

### Fixed
- Файловая архивация: промежуточная копия во временном каталоге `%TEMP%\AriaSignature` с удалением в `finally`; в каталоге архивов остаётся целевой `.rar`. Ретраи сервиса не повторяются при блокировке или ошибке доступа к исходнику/временному файлу. Сообщения об ошибке различают чтение исходника и запись временной копии.
- Несколько резервных копий одновременно: в UI отображаются все запущенные задачи; планировщик за один тик параллельно выполняет все попавшие в окно cron задачи (`Task.WhenAll`).
- Текст пустого списка вкладки «Активные задачи» без уточнения в скобках.
- Окно панели: нативный WPF‑оверлей при старте до готовности WebView2 и до загрузки `http://127.0.0.1…`; более заметная HTML‑заглушка; короткий таймаут запроса порта API; таймаут инициализации WebView2 (2 мин) с сообщением.

### Docs
- Обновлены `API.md` (cron по умолчанию, прогрев, история, вкладка активных задач) и версии документов.

## [0.2.9] - 2026-05-02

### Changed
- Подсказка настроек: только про перезапуск для смены порта; расписание обновления дисков применяется сразу через перепланирование Quartz.
- UI: сообщение после сохранения настроек отражает мгновенное применение расписания дисков.

### Fixed
- Файловая архивация: сообщение «файл занят» показывается только при реальной ошибке совместного доступа (HRESULT / точная фраза), без подмены других `IOException`.

## [0.2.8] - 2026-05-02

### Changed
- Подсказка в `GET /settings` (`note`): формулировка «Расписание обновления дисков» без упоминания SMART.
- Версия продукта `0.2.8` синхронизирована в .NET, web и установщике.

### Fixed
- Файловая архивация: уникальные имена временной копии и RAR по штампу с миллисекундами — безопасные параллельные запуски нескольких задач в одну папку.

## [0.2.7] - 2026-05-02

### Changed
- В таблицах задач и журнала отображается текст пути к папке рядом с кнопкой «Открыть папку»; колонка в «Настройка задач» переименована в «Путь к базе / База данных» (для MSSQL — имя базы).
- Добавлена колонка «Последний запуск» и индикатор «Выполняется…» на время ручного запуска архивации.
- Раздел дисков: заголовок истории «История дисков»; убраны лишняя подсказка в тулбаре и блок SMART из карточки диска.
- Версия продукта `0.2.7` синхронизирована в .NET, web и установщике.

### Fixed
- Убрано двойное подчёркивание (две линии) под вкладками раздела «Архивация» при выборе «Журнал».
- MSSQL: временный `.bak` создаётся с уникальным именем (без удаления занятого файла), снижена гонка «file is being used by another process»; сообщения об ошибках ввода-вывода переводятся на русский.

## [0.2.6] - 2026-05-01

### Added
- В API добавлены операции очистки SMART-истории:
  - `DELETE /api/v1/disks/{id}/smart` — очистка по выбранному диску;
  - `DELETE /api/v1/disks/smart` — глобальная очистка истории.
- В интерфейс добавлены кнопки очистки SMART-истории с подтверждением действия.

### Changed
- Заголовок раздела деталей переименован в `Карточка диска`.
- Терминология UI унифицирована на формулировку `Диски`.
- Версия продукта синхронизирована до `0.2.6` во всех контурах: .NET, web, installer и документация.

### Fixed
- Устранен сценарий финальной ошибки инсталлятора при позднем старте службы.
- Добавлена обработка `sc create` с кодом `1078` через fallback (альтернативный `DisplayName` и запуск без `DisplayName`).
- Усилен self-heal путь регистрации службы из UI.

### Docs
- Документация приведена к единому русскоязычному техническому стилю.
- Актуализированы `API`, `USER_GUIDE`, `DEVELOPER_GUIDE`, `INSTALLER`, `RELEASE_GATE`, `INSTRUCTIONS`.
- `DEVELOPMENT_NOTES` и `CHANGELOG` синхронизированы с релизом `0.2.6`.

## [0.2.5] - 2026-05-01

### Changed
- Существенно ускорен старт службы и выход UI из экрана ожидания.
- API перенесен в раннюю фазу запуска, инициализация БД переведена в фоновый этап с таймаутом и диагностикой.

### Fixed
- Снижены риски `database is locked` за счет `WAL`, `busy_timeout`, `synchronous=NORMAL` и retry-политики.
- Стабилизирован installer: WebView2 offline runtime, корректный цикл остановки/переустановки компонентов.

### Docs
- Обновлены ключевые документы под service-first модель и релиз `0.2.5`.

## [0.2.4] - 2026-04-30

### Fixed
- Возвращен единый путь запуска через `AriaSignatureService` без локального fallback-процесса.
- Стабилизирован старт API при нестабильной инициализации SQLite.
- Усилен контур reinstall/uninstall с полной очисткой каталога установки.

## [0.2.3] - 2026-04-30

### Added
- Внедрена стратегия `smartctl-first` для телеметрии с fallback по WMI и Storage.
- В модель и API добавлены observability-поля телеметрии.

### Fixed
- Исправлены сценарии черного/белого экрана при поздней готовности API.
- Устранены ошибки миграции SQLite при обновлении.

## [0.2.2] - 2026-04-29

### Added
- Добавлен низкоуровневый канал телеметрии через `smartctl`.
- Добавлены пресеты SMART-расписания и улучшена валидация формы задач.

### Changed
- Выравнены ключевые UI-потоки и русскоязычные статусы службы.

## [0.2.1] - 2026-04-29

### Fixed
- Стабилизирован запуск API и совместимость Swagger.
- Усилен путь регистрации службы в installer.

### Docs
- Синхронизированы версии артефактов и документации.

## [0.2.0] - 2026-04-28

### Added
- Введен новый UI-слой на React SPA внутри WebView2.
- Реализованы базовые сценарии панели: диски, архивация, настройки, раздел «О программе».
- Добавлены host-интеграции UI: выбор файлов/папок, взаимодействие с сервисом, стартовые операторские сценарии.

### Changed
- Закреплена service-first модель: исполнение в службе, UI как операторская консоль.
- Обновлены контракты API под практические сценарии диагностики и архивации.

### Docs
- Базовая документация приведена к целостному русскоязычному виду.

## [0.1.x] - 2026-04 

### Added
- Сформирован архитектурный каркас проекта и разбиение на Domain / Application / Infrastructure / API / Service / UI.
- Запущен локальный API внутри Windows-службы.
- Реализован сбор телеметрии дисков и плановое обновление.
- Реализован pipeline резервного копирования и хранение данных в SQLite.
- Добавлены CRUD-операции задач, журнал выполнения, базовые тесты API.
- Добавлен installer-контур и release-gate скрипт.

### Changed
- UI-панель последовательно эволюционировала от базовой MVVM-оболочки к интерактивному SPA-опыту.
- Локализация продукта приведена к русскому языку для UI и installer-сценариев.

### Fixed
- Поэтапно устранены критические сбои старта UI/службы, проблемы tray-потока и нестабильности service lifecycle.
- Улучшена идемпотентность installer-операций stop/delete/create службы.

### Docs
- Сформирован полный комплект эксплуатационной и технической документации.

## [0.0.1] - 2026-04-27

### Added
- Инициализирован репозиторий и базовая структура решения.
